import 'package:flutter/material.dart';

var kSizedBox = const SizedBox(
  height: 8,
);

var kContentPadding = const EdgeInsets.all(10);

var kTitleText = const Center(
  child: Text('API CRUD'),
);

var kFloatingButtonIcon = const Icon(Icons.add);